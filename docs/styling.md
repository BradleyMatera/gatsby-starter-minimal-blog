
# Styling

Primary stylesheets:
- `src/styles/global.css` - global layout + typography + footer.
- `src/styles/store.css` - store + portal UI.
- `src/styles/cyberpunk-nav.css` - header/nav look.

Use these files to adjust spacing, colors, and layout.

## Read next
- `content-authoring.md`
