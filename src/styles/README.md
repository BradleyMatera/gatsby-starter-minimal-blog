# styles

Global styling layer.

Current files:
- `global.css` large shared layout and page styling
- `site-chrome.css` header, footer, and shell chrome
- `vertical-nav.css` navigation layout and behavior
- `store.css` store-specific styling
- `utilities.css` reusable utility classes
- `media.css` media-specific helpers

Next cleanup target: split `global.css` by feature ownership.
