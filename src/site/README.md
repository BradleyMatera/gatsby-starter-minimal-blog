# site

Site-wide shared code.

Use this for pieces that are reused across multiple features or are part of the global site shell.
- `components/` cross-feature site components
- `accents/` decorative shared accents
- `hooks/` shared hooks used by multiple features or UI primitives
- `icons/` shared icon library

Rule: do not put feature-specific business logic here.
