# static

Bundled static assets served directly by Gatsby.

Use this only for assets that must ship with the site at build time.
Large or frequently changing media is a better candidate for external object storage.
