# Netlify functions

Serverless functions used by the site.

Keep each function self-contained and avoid coupling them to page-specific UI code.
Shared logic should move into a reusable server-side helper only when duplication is real.
